<Window x:Class="StudentNameWPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FUMiniHotelSystem - Hotel Management System" 
        Height="800" 
        Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="🏨 FUMiniHotelSystem" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                    <TextBlock Text="{Binding CurrentUser.CustomerFullName, StringFormat='Welcome, {0}'}" 
                               FontSize="14" Foreground="#666666" Margin="0,5,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="📊 Realtime Dashboard" 
                            Click="OpenRealtimeDashboard_Click"
                            Padding="15,8" 
                            Background="#4ECDC4" 
                            Foreground="White" 
                            BorderThickness="0" 
                            Cursor="Hand"
                            Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#4ECDC4"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45B7D1"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="Logout" 
                                Command="{Binding LogoutCommand}"
                                Padding="15,8" 
                                Background="#FF6B6B" 
                                Foreground="White" 
                                BorderThickness="0" 
                                Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FF6B6B"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FF5252"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Navigation -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
            <StackPanel Margin="20">
                    <TextBlock Text="Navigation" FontSize="16" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333333"/>
                
                <Button Content="📊 Dashboard" 
                          Command="{Binding NavigateCommand}" 
                            CommandParameter="Dashboard"
                            Style="{StaticResource NavigationButtonStyle}"
                            Margin="0,0,0,10"/>
                
                    <Button Content="👥 Customers" 
                          Command="{Binding NavigateCommand}" 
                          CommandParameter="Customers"
                            Style="{StaticResource NavigationButtonStyle}"
                            Margin="0,0,0,10"
                            Visibility="{Binding CurrentUser.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                    <Button Content="🏨 Rooms" 
                          Command="{Binding NavigateCommand}" 
                          CommandParameter="Rooms"
                            Style="{StaticResource NavigationButtonStyle}"
                            Margin="0,0,0,10"
                            Visibility="{Binding CurrentUser.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                
                    <Button Content="📋 Bookings" 
                          Command="{Binding NavigateCommand}" 
                            CommandParameter="Bookings"
                            Style="{StaticResource NavigationButtonStyle}"
                            Margin="0,0,0,10"/>
                
                    <Button Content="📈 Reports" 
                          Command="{Binding NavigateCommand}" 
                          CommandParameter="Reports"
                            Style="{StaticResource NavigationButtonStyle}"
                            Margin="0,0,0,10"
                            Visibility="{Binding CurrentUser.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>

            <!-- Content Area -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    
                    <!-- Dashboard View -->
                    <StackPanel Visibility="{Binding CurrentView, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Dashboard}">
                        <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333333"/>
                        
                        <!-- Statistics Cards -->
                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="👥" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Customers.Count}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#667eea"/>
                                    <TextBlock Text="Total Customers" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="5,0">
                                <StackPanel>
                                    <TextBlock Text="🏨" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Rooms.Count}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#4CAF50"/>
                                    <TextBlock Text="Total Rooms" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="5,0">
                                <StackPanel>
                                    <TextBlock Text="📋" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Bookings.Count}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF9800"/>
                                    <TextBlock Text="Total Bookings" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="💰" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Bookings.Count, StringFormat=C}" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#9C27B0"/>
                                    <TextBlock Text="Revenue" FontSize="14" HorizontalAlignment="Center" Foreground="#666666"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                        
                        <!-- Recent Bookings -->
                        <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="20">
                                <StackPanel>
                                <TextBlock Text="Recent Bookings" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="#333333"/>
                                
                                <DataGrid ItemsSource="{Binding Bookings}" 
                                          AutoGenerateColumns="False" 
                                          CanUserAddRows="False" 
                                          CanUserDeleteRows="False"
                                          IsReadOnly="True"
                                          GridLinesVisibility="Horizontal"
                                          HeadersVisibility="Column"
                                          Background="White"
                                          BorderThickness="0">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingID}" Width="80"/>
                                        <DataGridTextColumn Header="Room" Binding="{Binding RoomID}" Width="80"/>
                                        <DataGridTextColumn Header="Check In" Binding="{Binding CheckInDate, StringFormat=MM/dd/yyyy}" Width="100"/>
                                        <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutDate, StringFormat=MM/dd/yyyy}" Width="100"/>
                                        <DataGridTextColumn Header="Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="100"/>
                                        <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" Width="80"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Customers View -->
                    <StackPanel Visibility="{Binding CurrentView, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Customers}">
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="Customer Management" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Add Customer" 
                                        Command="{Binding AddCustomerCommand}"
                                        Padding="15,8" 
                                        Background="#4CAF50" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Margin="0,0,10,0"
                                        Cursor="Hand"/>
                                
                                <Button Content="Edit Customer" 
                                        Command="{Binding EditCustomerCommand}"
                                        Padding="15,8" 
                                        Background="#2196F3" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Margin="0,0,10,0"
                                        Cursor="Hand"/>
                                
                                <Button Content="Delete Customer" 
                                        Command="{Binding DeleteCustomerCommand}"
                                        Padding="15,8" 
                                        Background="#F44336" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Cursor="Hand"/>
                            </StackPanel>
                        </Grid>
                        
                        <DataGrid ItemsSource="{Binding Customers}" 
                                SelectedItem="{Binding SelectedCustomer}"
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  Background="White"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}" Width="60"/>
                                <DataGridTextColumn Header="Full Name" Binding="{Binding CustomerFullName}" Width="200"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}" Width="250"/>
                                <DataGridTextColumn Header="Telephone" Binding="{Binding Telephone}" Width="120"/>
                                <DataGridTextColumn Header="Birthday" Binding="{Binding CustomerBirthday, StringFormat=MM/dd/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding CustomerStatus}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <!-- Rooms View -->
                    <StackPanel Visibility="{Binding CurrentView, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Rooms}">
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="Room Management" FontSize="24" FontWeight="Bold" Foreground="#333333"/>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Add Room" 
                                        Command="{Binding AddRoomCommand}"
                                        Padding="15,8" 
                                        Background="#4CAF50" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Margin="0,0,10,0"
                                        Cursor="Hand"/>
                                
                                <Button Content="Edit Room" 
                                        Command="{Binding EditRoomCommand}"
                                        Padding="15,8" 
                                        Background="#2196F3" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Margin="0,0,10,0"
                                        Cursor="Hand"/>
                                
                                <Button Content="Delete Room" 
                                        Command="{Binding DeleteRoomCommand}"
                                        Padding="15,8" 
                                        Background="#F44336" 
                                        Foreground="White" 
                                        BorderThickness="0" 
                                        Cursor="Hand"/>
                            </StackPanel>
                        </Grid>
                        
                        <DataGrid ItemsSource="{Binding Rooms}" 
                                SelectedItem="{Binding SelectedRoom}"
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  Background="White"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="60"/>
                                <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="120"/>
                                <DataGridTextColumn Header="Description" Binding="{Binding RoomDescription}" Width="300"/>
                                <DataGridTextColumn Header="Capacity" Binding="{Binding RoomMaxCapacity}" Width="80"/>
                                <DataGridTextColumn Header="Price" Binding="{Binding RoomPricePerDate, StringFormat=C}" Width="120"/>
                                <DataGridTextColumn Header="Type ID" Binding="{Binding RoomTypeID}" Width="80"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding RoomStatus}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <!-- Bookings View -->
                    <StackPanel Visibility="{Binding CurrentView, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Bookings}">
                        <TextBlock Text="Booking History" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333333"/>
                        
                        <DataGrid ItemsSource="{Binding Bookings}" 
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  CanUserDeleteRows="False"
                                  IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column"
                                  Background="White"
                                  BorderBrush="#E0E0E0"
                                  BorderThickness="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Booking ID" Binding="{Binding BookingID}" Width="80"/>
                                <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="150"/>
                                <DataGridTextColumn Header="Room Name" Binding="{Binding RoomName}" Width="100"/>
                                <DataGridTextColumn Header="Check In" Binding="{Binding CheckInDate, StringFormat=MM/dd/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOutDate, StringFormat=MM/dd/yyyy}" Width="100"/>
                                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount, StringFormat=C}" Width="120"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus}" Width="80"/>
                                <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat=MM/dd/yyyy}" Width="120"/>
                                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>

                    <!-- Reports View -->
                    <StackPanel Visibility="{Binding CurrentView, Converter={StaticResource StringToVisibilityConverter}, ConverterParameter=Reports}">
                        <TextBlock Text="Reports &amp; Analytics" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333333"/>
                        
                        <!-- Export Buttons -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                            <Button Content="📊 Export HTML Report" 
                                    Command="{Binding ExportReportCommand}"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#3498db"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#2980b9"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            
                            <Button Content="📈 Export Charts" 
                                    Command="{Binding ExportChartCommand}"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#e74c3c"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#c0392b"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            
                            <Button Content="📄 Export PDF" 
                                    Command="{Binding ExportPDFCommand}"
                                    Margin="0,0,10,0"
                                    Padding="15,10"
                                    Background="#f39c12"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#e67e22"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            
                            <Button Content="📋 Export Excel" 
                                    Command="{Binding ExportExcelCommand}"
                                    Padding="15,10"
                                    Background="#27ae60"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#229954"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                        
                        <!-- Report Description -->
                        <Border Background="#f8f9fa" 
                                BorderBrush="#dee2e6" 
                                BorderThickness="1" 
                                Padding="20" 
                                Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="📊 Export Features" FontSize="18" FontWeight="Bold" Margin="0,0,0,10" Foreground="#2c3e50"/>
                                <TextBlock Text="• HTML Report: Comprehensive business report with statistics and analysis" Margin="0,0,0,5" Foreground="#34495e"/>
                                <TextBlock Text="• Charts: Revenue trends, customer distribution, room performance, and occupancy rates" Margin="0,0,0,5" Foreground="#34495e"/>
                                <TextBlock Text="• PDF: Professional formatted report for presentations and documentation" Margin="0,0,0,5" Foreground="#34495e"/>
                                <TextBlock Text="• Excel: Detailed data analysis with multiple sheets for further processing" Margin="0,0,0,5" Foreground="#34495e"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- Quick Stats -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#ecf0f1" Padding="15" Margin="5">
                                <StackPanel>
                                    <TextBlock Text="Total Revenue" FontSize="12" Foreground="#7f8c8d" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Bookings, Converter={StaticResource TotalRevenueConverter}}" 
                                               FontSize="20" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Grid.Column="1" Background="#ecf0f1" Padding="15" Margin="5">
                                <StackPanel>
                                    <TextBlock Text="Total Bookings" FontSize="12" Foreground="#7f8c8d" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Bookings.Count}" 
                                               FontSize="20" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Background="#ecf0f1" Padding="15" Margin="5">
                                <StackPanel>
                                    <TextBlock Text="Total Customers" FontSize="12" Foreground="#7f8c8d" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Customers.Count}" 
                                               FontSize="20" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="3" Background="#ecf0f1" Padding="15" Margin="5">
                                <StackPanel>
                                    <TextBlock Text="Total Rooms" FontSize="12" Foreground="#7f8c8d" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding Rooms.Count}" 
                                               FontSize="20" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                    </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
